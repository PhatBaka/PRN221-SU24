@* @page
@model UI.Pages.Orders.OrderConfirmationModel
@{
    ViewData["Title"] = "Order Confirmation";
    Layout = "_Layout"; // Assuming you have a shared layout file
}

<h1 class="my-4">Order Confirmation</h1>

<div class="card">
    <div class="card-header">
        <h5 class="card-title">Order Details</h5>
    </div>
    <div class="card-body">
        <dl class="row">
            <dt class="col-sm-3">Order ID:</dt>
            <dd class="col-sm-9">@Model.Order.OrderId</dd>

            <dt class="col-sm-3">Order Date:</dt>
            <dd class="col-sm-9">@Model.Order.OrderDate.ToString("yyyy-MM-dd HH:mm:ss")</dd>

            <dt class="col-sm-3">Customer ID:</dt>
            <dd class="col-sm-9">@Model.Order.CustomerId</dd>

            <dt class="col-sm-3">Total Items:</dt>
            <dd class="col-sm-9">@Model.Order.OrderDetails.Count</dd>

            @* <dt class="col-sm-3">Total Price:</dt>
            <dd class="col-sm-9">@Model.Order.TotalPrice.ToString("C")</dd> 
        </dl>
    </div>
</div>

<div class="mt-4">
    <a asp-page="./Index" class="btn btn-primary">Back to Home</a>
</div>
 *@