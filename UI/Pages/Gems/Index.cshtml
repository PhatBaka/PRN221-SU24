@page
@model UI.Pages.Gems.IndexModel
@{
    Layout = "_ManagerLayout";
    
    var prevDisabled = !Model.Gems.HasPreviousPage ? "disabled" : "";
    var nextDisabled = !Model.Gems.HasNextPage ? "disabled" : "";
}

<style>
    .table > thead > tr > th, .table > tbody > tr > th, .table > tfoot > tr > th, .table > thead > tr > td, .table > tbody > tr > td, .table > tfoot > tr > td {
        padding: 8px;
        line-height: 1.42857143;
        vertical-align: middle;
        border-top: 1px solid #ddd
    }
</style>

<section id="center" class="center_shop">
    <div class="container">
        <div class="row">
            <div class="center_shop_t clearfix">
                <div class="col-sm-6">
                    <div class="center_shop_tl clearfix d-flex align-items-center">
                        <h4 class="mgt"><a class="button" asp-page="/Gems/Create">CREATE</a></h4>
                    </div>
                </div>
                <div class="col-sm-6">
                    <div class="center_shop_tr text-right clearfix">
                        <h6 class="col_2">You are here: <a asp-asp-page="Gems/Index">Home</a> / Gem</h6>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<section id="center" class="center_shop">
    <div class="container">
        <div class="row">
            <div class="center_shop_t clearfix">
                <div class="center_shop_tl clearfix">
                    <table class="table table-striped table-center">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>Cetificate Code</th>
                                <th>Weight</th>
                                <th>Clarity</th>
                                <th>Color</th>
                                <th>Purity</th>
                                <th>Shape</th>
                                <th>Cut</th>
                                <th>Sell Price</th>
                                <th>Buy Price</th>
                                <th>Created Date</th>
                                <th></th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var gem in Model.Gems)
                            {
                                <tr class="d-flex justify-content-center">
                                    <td>@gem.MaterialId</td>
                                    <td>@gem.CertificateCode</td>
                                    <td>@gem.Weight</td>
                                    <td>@gem.Clarity</td>
                                    <td>@gem.Color</td>
                                    <td>@gem.Purity</td>
                                    <td>@gem.Shape</td>
                                    <td>@gem.Cut</td>
                                    <td>@gem.SellPrice</td>
                                    <td>@gem.BuyPrice</td>
                                    <td>@gem.CreatedDate</td>
                                    <td>
                                        <img src="data:image/jpeg;base64,@Convert.ToBase64String(gem.MaterialImageData)" alt="@gem.CertificateCode" style="max-width: 100px; max-height: 100px;" />
                                    </td>
                                    <td>
                                    <td>
                                        <a class="button" asp-page="/Gems/Detail" asp-route-id="@gem.MaterialId">Details</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>

                </div>
                <div class="enquiry_1 text-center clearfix">
                    <nav aria-label="Page navigation">
                        <ul class="pagination justify-content-center">
                            <a asp-page="./Index"
                               asp-route-pageIndex="@(Model.Gems.PageIndex - 1)"
                               asp-route-currentFilter="@Model.CurrentFilter"
                               class="btn btn-primary @prevDisabled">
                                Previous
                            </a>
                            <a asp-page="./Index"
                               asp-route-pageIndex="@(Model.Gems.PageIndex + 1)"
                               asp-route-currentFilter="@Model.CurrentFilter"
                               class="btn btn-primary @nextDisabled">
                                Next
                            </a>
                        </ul>
                    </nav>
                </div>
            </div>
        </div>
    </div>
</section>
